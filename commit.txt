Fix Pacific arc routing, collapse UI, API_BASE auto-detect, deploy scaffolding — 2026-06-28

solar_seismic.html:
  - arcPoints: sine curve replaced with 2-segment angle-bracket tent (start→apex→end)
  - adjLon(): all coordinates shifted to Pacific-copy frame (lon > 0 → lon - 360)
  - Map centered at [20, -160] (Pacific center), worldCopyJump: false — eliminates
    tile-copy drift on zoom
  - Partner and seismic node markers, station dots, flyTo all use adjLon() so
    nodes stay anchored to their arcs when zooming into Oceania
  - API_BASE auto-detect: localhost → relative URLs; any other host →
    https://igpulse.imscribe.com; error now shows in sidebar not over map
  - Collapse toggles: header (−/⚡ show header), sidebar (▶/◀ edges tab)
    both call map.invalidateSize() to reflow

geo_viz.py:
  - aurora_north_peak → Fairbanks AK (64.8°N, -147.9°W)
  - aurora_south_peak → Ushuaia (-54.8°S, -68.3°W)
  - aurora_ns_asymmetry / aurora_oval_extent → Fairbanks anchor
  - neutron_monitor_oulu / forbush → Climax CO (-106.2°W) anchor
  All extraplanetary partner nodes now in Western hemisphere; no Eastern-hemisphere
  short arcs appear on the right side of the Pacific-centered map

Previous entries:
  - Streams 38-43 (extraplanetary + polar geomag)
  - All-domain seismic coupling (950 edges, 5 categories)
  - Coastal seismic station fix
  - CORS + Dockerfile + fly.toml
  - 49h/spin-½ SIC-POVM §7.3 addition
