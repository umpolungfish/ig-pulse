I reconciled the ig-pulse prose with what the analysis actually shows, and made
the Dual-Link SIC-POVM status explicit (README.md, USER_GUIDE.md).

Corrections:
  - Dropped the "unchanging adjacency matrix / edge invariants / structural
    constants" framing. The out-of-sample gate (ig_pulse/oos.py) shows the
    coupling graph is non-stationary. What is invariant is the native-B floor
    (over-dispersion ~3.4x, replicates out-of-sample); the wiring is weather.
    Kept the defensible sense of "atemporal": the coupling is contemporaneous,
    not a directed sequence (ig_pulse/braid.py, directed flow below null).
  - Flagged the r=1.000 / lambda=16469s edges as degenerate Pearson-lag-coupler
    artifacts, superseded by the signed broadcast fan
    (ig_pulse/broadcast_coupling.py).
  - Fixed the SIC section: the frame is a genuine d=12 SIC (numerically exact,
    144 overlaps at 1/13 to machine precision), but the 44-stream apparatus is a
    partial heteroskedastic POVM (24/144 directly addressed), NOT informationally
    complete. A chi2 p~1.00 is a consistency check with maximal mixedness, not a
    completeness proof.
  - Corrected the "2.7% at exact B-state (no alerts)" line: those are dead
    collections from a dated ~27-hour outage, not B-floor readings.
  - Native-B is validated by the out-of-sample over-dispersion gate, not by the
    in-sample near-I/12 diagonal.

Dual-Link SIC-POVM status, stated plainly for readers and for any model fed this
file: the d=12 frame is numerically exact and structurally derived (general
SIC/Zauner open); the Dual-Link closure (mu.delta=id) is an IG-internal
construction verified in the actor fin3r and formalised in p4rakernel, not
peer-reviewed; ig-pulse is the inner measurement side only and does not close the
loop, which is the same reason the Omega braid promotion fails in the observer
and lives in the actor.

Run: python -m ig_pulse.oos
