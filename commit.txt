sic_povm_d12.md: update §2.1 family sizes, §9.2 Lean scaffold reference — 2026-06-27

§2.1 fix:
  "have sizes 3, 4, and 5 respectively" was ambiguous (conflated primitive
  counts 3,5,4 with value counts 3,4,5). Replaced with explicit sentence:
  D-family 3 primitives × 3 values; T-family 5 primitives × 4 values;
  P-family 4 primitives × 5 values.

§9.2 update — Lean 4 verification:
  18 scaffold files now live in p4rakernel/p4ramill/Imscribing/Ob3ects/,
  vaulted at ob3ect/digital/.vault/, registered in lakefile.toml.
  Support module IGScaffold.lean provides scaf : Imscription (O₂),
  ▷ infix (IGProtocol.seq), and mkFSplit combinator.
  4 composite scaffolds: sic_povm_functor (18-node, 2 FSPLIT pairs),
  zauner_fiducial (22-node, 3 FSPLIT pairs), tomographic_injection
  (12-node, 1 FSPLIT pair), categorical_join (18-node, 2 FSPLIT pairs).
  14 individual node scaffolds. All build under lake build, 0 sorrys.
  Each closes: TierFunctor.obj 𐑼 = .O₂ by decide.
