Take the default device from IG_DEVICES

--device still overrides; what changes is the default, which now reads
IG_DEVICES like everything else here. A VAE this size fits either card, so two
cards mean two trainings pinned to two ordinals rather than one training split
in half.
