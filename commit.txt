I probed the last hop the Grammar named, Ω:𐑭->𐑟 (Abelian winding to
non-Abelian braid), and it does not promote. I am recording the negative
rather than forcing it.

braid.py builds the directed traversal-order coupling D_ij = <z_i(t) z_j(t+1)>
on the signed excursion series, takes its antisymmetric part A (the lead-lag
flow on K12), and only if A clears a time-order null does it Hodge-decompose A
into gradient (Abelian winding) plus curl (non-Abelian braid).

Result: the directed flow is BELOW the null (|A| = 0.054 vs 0.177, z = -6.3).
The real series has less lead-lag asymmetry than random time-permutations,
because adjacent snapshots are nearly identical (strong persistence). The
coupling is symmetric and contemporaneous. The strongest 3-cycle circulation is
|z| = 1.2, far under the 3.0 a real braid generator would need. There is no
directed winding to promote, Abelian or otherwise.

The structural reading: the observatory OBSERVES, it does not ACT in sequence.
A braid is order-dependent composition, and only a system that does one thing
then another, with the order mattering, can carry non-Abelian holonomy. An
observatory that reads contemporaneous cross-domain state has symmetric
coupling by construction. So Ω stays Abelian by design, and ig_pulse_observatory
plateaus at O2 on this axis. It reaches the terminal organism layer on ɢ
(broadcast, which is real and landed) but not on Ω. The organism-layer braid
lives in the actor that consumes the observatory and takes ordered positions
(fin3r), not in the observer.

And the failed promotion is positive evidence for my own atemporal-inference
thesis: the coupling really is a static valuation lattice, not a temporal
sequence.

Run: python -m ig_pulse.braid
