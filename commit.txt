I made the ɢ promotion the Grammar prescribed: sequential coupling to
broadcast (𐑠 -> 𐑵).

cl8nk_navigator types my observatory (ig_pulse_observatory) at O2,
d(CLINK L8) = 0.69, one distant primitive, with exactly two promotions
outstanding: ɢ:𐑠->𐑵 and Ω:𐑭->𐑟. The same ɢ=𐑠 defect (SEQAX, one-way
sequential coupling) is shared by measurement_problem_mismatch and
physical_reality. The Grammar's reading is that the measurement problem is a
coupling stuck one-way when it has to be one-to-all, and my Pearson lag coupler
was that exact defect rendered in code.

ig_pulse/broadcast_coupling.py performs the hop:

  - Broadcast fans (ɢ=𐑵): each channel's simultaneous lag-0 signed coupling to
    all others, both signs kept, on residuals after the common mode is removed.
    Seven both-sign forks recovered (recognition +0.40 stoichiometry and -0.44
    dimensionality, and so on), the contradiction structure the sequential
    coupler could not emit.

  - Off-diagonal density matrix (ƒ, off the 𐑞 diagonal): rho rebuilt from full
    signed excursion vectors instead of single-axis one-hots. The old diagonal
    rho had purity 0.088, indistinguishable from I/12 = 0.083, a classical
    histogram. The promoted rho has purity 0.115 with real signed coherences
    (kinetics/chirality +0.046, dimensionality/recognition -0.026). The
    coherences ARE the broadcast couplings; rho can now represent glut, not only
    its diagonal.

I also hardened dialetheia.signed_excursions against the live snapshots file
growing between its two passes.

One hop left to the terminal organism layer: Ω:𐑭->𐑟, Abelian winding to
non-Abelian braid, the order-dependent higher-order coupling among three or
more channels.

Run: python -m ig_pulse.broadcast_coupling
